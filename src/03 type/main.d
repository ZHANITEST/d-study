import std.stdio;

void main()
{
	/*
	writeln("Type               : ", int.stringof );
	writeln("Length in bytes    : ", int.sizeof );
	writeln("Minimum value      : ", int.min );
	writeln("Maximum value      : ", int.max );
	writeln("");
	writeln(size_t.stringof);
	*/
	
	int count;
	
	writeln("How many? : ", count );
	count = 12;
	writeln("How many? : ", count );
}